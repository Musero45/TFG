function interf = unitInterf(Sinterf)

% noneInterf Calculates the inflow interference. (1 model)
% 
%   interf = unitInterf(Sinterf) considers the inflow interference, assuming
%   there is always modification on the element velocities caused by the
%   induced one, independently on the flight codition.

interf = 1;

end